[toolchain]
package_manager = "yarn"

[features]
resolution = true
skip-lint = false

[programs.devnet]
metaplex_core = "HUBjpQsy6x6ydWFq1fatHek6ktKW49uP55RdZcgzR6Ed"

[registry]
url = "https://api.apr.dev"

[provider]
cluster = "devnet"
wallet = "~/.config/solana/id.json"

[scripts]
test = "yarn run ts-mocha -p ./tsconfig.json -t 1000000 \"tests/**/*.ts\""
mint_asset = "ts-node -T scripts/mint_asset.ts"
transfer_asset = "ts-node -T scripts/transfer_asset.ts"


